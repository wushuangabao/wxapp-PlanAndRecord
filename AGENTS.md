# AGENTS.md

本文件是本仓库 AI Agent 的默认协作规则。处理任务前先读取本文件；目标目录有更深层 `AGENTS.md` 或同类 instructions 时，以其更具体的规则为准。系统、开发者和当前用户的明确指令优先。

## 沟通与范围

- 使用中文沟通、中文文档和中文注释；标识符保持英文；文本文件使用 UTF-8。
- 只修改完成当前目标所必需的文件；工作区已有的无关改动属于用户，不能覆盖、还原或混入提交。
- 不确定事项只有会改变产品设计、验收标准、写入范围或安全边界时才询问；其余事实先自行查证。

## 文档驱动

- 所有产品、数据模型、计时、日历、统计、同步、隐私或分期问题，先读 `docs/product-design.zh-CN.md`；它是产品设计基线。
- 新增或修改 wechat 的小微 AI Skill、原子接口或 Handoff 时，额外读 `docs/xiaowei-ai-handoff.zh-CN.md`；它是微信开发工具 AI 能力的接入规范，不定义面向用户的产品 AI 功能范围。
- 会实质改变用户可见行为、领域语义、数据契约、隐私/安全边界或正式范围的变更，先更新相应权威文档草案并取得用户确认；只读调查、已确认设计的 bug 修复、测试、构建和行为不变重构除外。
- 同一规则只在一处权威文档维护完整定义，其他位置简述并链接，避免复制后漂移。

## 开发规范

- 修改页面时同步检查对应的 `.js`、`.wxml`、`.wxss`、`.json` 与 `app.json`；不得覆盖既有页面、分包或配置。
- 有副作用的 AI/API 操作必须校验登录态、资源归属、参数合法性与幂等性；Agent 不得猜测 ID、权限或业务数据。
- 密钥、支付签名和敏感个人信息不得进入小程序代码、Skill 文件、日志、query 或 Handoff payload。
- Handoff payload 仅用于首屏预置；接力页必须能在 payload 缺失时，依据可信 ID 重新查询并安全降级。

## 验证与交付

- 先做与改动相称的验证：普通改动回归小程序页面；AI 改动同时在 AI 编译模式验证接口 schema、失败分支和 Handoff 链路。
- 涉及登录、支付、数据同步、OCR 或模型服务时，报告已验证与未验证的边界，不以本地 mock 替代生产验收。
- 交付时说明改动文件、验证命令/结果和仍需用户手动确认的体验；未运行的验证必须明确说明。
